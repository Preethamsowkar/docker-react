sudo: required
services:
    -docker

before_install:
    - docker build -t preethamsowkar/docker-react -f Dockerfile.dev .

scripts:
    - docker run preethamsowkar/docker-react npm run test -- --coverage